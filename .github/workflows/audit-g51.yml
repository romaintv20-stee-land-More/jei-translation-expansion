name: Audit G51 Minecraft 26.2

on:
  workflow_dispatch:
  push:
    branches: ['work/g51-mc26.2-audit']
    paths:
      - 'scripts/audit_26_2.py'
      - '.github/workflows/audit-g51.yml'
      - 'upstream/sources/26.1.2/en_us.json'
      - 'upstream/minecraft-26.1.2-language-scope.json'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Audit maintained Minecraft 26.2 snapshot
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/audit_26_2.py
